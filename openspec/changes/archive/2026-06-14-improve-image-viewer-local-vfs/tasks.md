## 1. Thumbnail Grid

- [x] 1.1 Remove classification metadata, AI filename, confidence, and tag text from primary masonry grid thumbnails.
- [x] 1.2 Render each masonry item as a keyboard-accessible thumbnail button with loading and error states.
- [x] 1.3 Remove grid-card quick requeue controls so secondary actions do not appear on thumbnail tiles.

## 2. Local VFS Image Viewer

- [x] 2.1 Replace the metadata-heavy media detail panel with a focused image viewer overlay.
- [x] 2.2 Build the viewer image source from the local VFS file endpoint using the selected media hash.
- [x] 2.3 Ensure the viewer does not use the original page source URL for image display.
- [x] 2.4 Keep viewer actions for close, download, and optional requeue classification/renaming.

## 3. Verification

- [x] 3.1 Format changed DevTools Panel files.
- [x] 3.2 Run focused lint or type checks for the changed DevTools Panel TypeScript files.
- [x] 3.3 Confirm repository-wide checks that cannot pass are blocked only by pre-existing unrelated issues.
